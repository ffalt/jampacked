query RateResult($id: ID!) {
    state(id: $id) {
        id
        rated
    }
}
mutation SetRateResult($id: ID!, $rating: Int!) {
    rate(id: $id, rating: $rating ) {
        id
        rated
    }
}
